apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: comment-data
  name: comment-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
